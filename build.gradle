plugins {
    id 'org.springframework.boot' version '3.1.1' // match your Spring Boot version
    id 'io.spring.dependency-management' version '1.1.0'
    id 'java'
}

bootJar {
    enabled = true       // Enable JAR generation
}

bootWar {
    enabled = false      // Disable WAR generation
}
